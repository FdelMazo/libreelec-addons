#!/bin/sh

. /etc/profile

# stop and remove container
docker stop docker.linuxserver.radarr

# remove image from cron updater list
sed -i '/radarr/d' /storage/.kodi/userdata/addon_data/docker.linuxserver.updater/update.sh

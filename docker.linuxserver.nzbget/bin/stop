#!/bin/sh

. /etc/profile

# stop and remove container
docker stop docker.linuxserver.nzbget

# remove image from cron updater list
sed -i '/nzbget/d' /storage/.kodi/userdata/addon_data/docker.linuxserver.updater/update.sh

#!/bin/sh

. /etc/profile

# stop and remove container
docker stop docker.linuxserver.grocy

# remove image from cron updater list
sed -i '/grocy/d' /storage/.kodi/userdata/addon_data/docker.linuxserver.updater/update.sh
